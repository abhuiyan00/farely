{
  "name": "farely",
  "private": true,
  "version": "0.1.0",
  "description": "Farely — a personal rideshare/delivery driver assistant for Wrocław (real-time offer scoring, demand-zone positioning, multi-app autopilot). Monorepo: apps/tester = web UI tester + domain engine, apps/android = Capacitor APK.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --prefix apps/tester run dev",
    "build": "npm --prefix apps/tester run build",
    "sync": "npm --prefix apps/android run sync",
    "apk": "npm run build && npm --prefix apps/android run apk",
    "cap:open": "npm --prefix apps/android run open"
  },
  "license": "MIT"
}
